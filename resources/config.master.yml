db:
  database-name: vectors

gemini:
  model: gemini-2.0-flash

prompt:
  extract-fields: resources/prompts/extract-fields.txt

kafka:
  brokers:
    - kafka:29092
  topics:
    parse-message:
      topic: parse-message
      group-id: parse-message-group
    update-message:
      topic: update-message
      group-id: update-message-group
